v2.5.1    (DO THIS)


Target layout
server.py            # orchestrator: routes, settings, dispatch to active backend
mlx_server.py         # MLX backend logic (local models from huggingface or other)
ollama_server.py       # Ollama backend logic
Step 1: Define the shared interface first
Before moving any code, decide the exact function signature both backend files will implement. Something like:

send_prompt(messages, stream=True) -> Iterator[str] (or whatever return shape your app.js streaming consumer already expects)
list_models() -> list[str] (so the UI's model dropdown can populate regardless of backend)
health_check() -> bool (optional, but useful once you have 2+ backends and want to show connection status)

This interface is the contract server.py will rely on — write it down before touching files, since both backend files need to match it exactly.
Step 2: Extract ollama_server.py

Move everything currently talking to localhost:11434 out of server.py into this new file
Wrap it to match the shared interface from Step 1
Keep Ollama's existing JSON-lines streaming parse logic here — it stays fully contained in this file

Step 3: Create mlx_server.py

New file, same interface shape as ollama_server.py
Talks to mlx_lm.server's OpenAI-compatible endpoint instead (different port, different streaming shape — SSE with delta.content)
list_models() here likely reads from a static config or a local models directory, since MLX server doesn't expose model discovery the way Ollama's /api/tags does

Step 4: Slim down server.py into a pure orchestrator

Add a backend setting (config value, env var, or UI-driven) that resolves to either ollama_server or mlx_server
Replace all direct backend calls with calls through the shared interface: active_backend.send_prompt(...), active_backend.list_models(), etc.
Routes/endpoints exposed to app.js stay the same — only what's behind them changes
No backend-specific logic (streaming parsing, ports, model listing quirks) should remain in this file

Step 5: Update app.js only where necessary

If both backends now return a normalized stream shape (from Step 1's interface), app.js likely needs zero changes
If the interface only normalizes at the Python level and something backend-specific still leaks to the frontend (e.g., model list format), adjust the fetch/parsing calls there — but this should be minimal if Step 1 was done right

Set 6: Upadate settings page

check over work


v3.5.1  (NOT TO BE WORKED ON)
- On middle top is a place where you pick Chat or Agent
    - Agent edits the attached context files/folders only
    - There are two modes: Plan and Execute
        - Plan mode allows the LLM to create a plan of action
        - Execute mode allows the LLM to run the planned actions or if none just run and plan some first
    - It gives ability for the LLM to run terminal commands